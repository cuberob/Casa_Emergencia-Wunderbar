<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rob.bryan.steven.hackathon2014" >

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:name="com.rob.bryan.steven.hackathon2014.HackathonApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.rob.bryan.steven.hackathon2014.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="activities.AlertsActivity"
            android:label="@string/title_activity_alerts" >
        </activity>
        <activity
            android:name="com.rob.bryan.steven.hackathon2014.activities.LoginActivity"
            android:label="@string/app_name" />

        <service
            android:name=".services.SensorDataService"
            android:exported="false" >
        </service>

    </application>

</manifest>
